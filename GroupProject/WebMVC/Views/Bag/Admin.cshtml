@model  WebMVC.BagCategoryService.BagCategory[]

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Admin</title>
</head>
<body>
    <div>
        <select>
            @foreach (var item in Model)
            {
                <option>
                    @Html.DisplayFor(modelItem => item.CateName)
                </option>
            }
        </select>
    </div>
</body>
</html>
